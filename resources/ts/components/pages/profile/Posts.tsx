import React, { FC, ReactElement } from 'react';
import Post from 'pages/general/Post';

const Posts: FC = (): ReactElement => {
    return (
        <section className='pd-b--lg pd-l--sm pd-r--sm'>
            {/* <Post namespace='profile' /> */}
        </section>
    );
};

export default Posts;
