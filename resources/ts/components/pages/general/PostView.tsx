import React, { FC, ReactElement } from 'react';

const PostView: FC = (): ReactElement => {
    return (
        <section>
            <h1>Post View</h1>
        </section>
    );
};

export default PostView;
